import SwiftUI

struct SearchView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Екран профілю")
                    .font(.title)
                // Тут додайте ваш контент
            }
            .navigationTitle("Профіль")
        }
    }
}
